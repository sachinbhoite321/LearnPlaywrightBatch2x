let arr = [1, 2, 3];

console.log(arr);

arr.push(10, 29, 30);// Adddint the element last
console.log(arr);

arr.pop();// Removing the value from the last index
console.log(arr);

arr.unshift(11, 12, 13) // Adding the element from index of 1 i,e start end
console.log(arr);

arr.shift();// Removing the element from index 1
console.log(arr);
